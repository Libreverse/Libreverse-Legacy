# Socket Firewall Enterprise config (not used in CI without Enterprise license).
# Local dev: use Socket Firewall Free (sfw-free) for npm/pip/cargo only — not bundle/bun.
# See documentation/dependency-rolling-autonomous.md

[firewall]
enabled = false
